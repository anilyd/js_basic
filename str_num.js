const snum = "123";
const cnum = Number(snum);
console.log(`string number convert into number ${cnum}:`);

// not a number (Nan)
const name="anil kumar";
const cname=Number(name);
console.log(`not a number result ${cname}`)








